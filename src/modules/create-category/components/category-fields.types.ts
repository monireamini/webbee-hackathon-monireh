import {ArrayHelpers} from 'formik';
import {categoryFieldPropTypes} from './category-field.types';

export interface categoryFieldsPropTypes {
  arrayHelpers: ArrayHelpers;
  fields: categoryFieldPropTypes[];
}
